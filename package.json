{
  "private": true,
  "name": "preact-web-componet-styles-in-shadow-dom",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src",
    "start": "npm run build && npm run serve"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "eslint": "^8.30.0",
    "eslint-config-preact": "^1.3.0",
    "preact-cli": "^3.4.2",
    "sirv-cli": "^2.0.2"
  },
  "dependencies": {
    "preact": "^10.11.3",
    "preact-custom-element": "^4.2.1",
    "preact-render-to-string": "^5.2.6",
    "preact-router": "^3.2.1"
  }
}
